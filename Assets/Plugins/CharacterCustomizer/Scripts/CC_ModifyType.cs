using UnityEngine;

namespace CC
{
    public enum CC_ModifyType
    {
        None, ShoulderWidth, HeadSize, LowerWaistSize, MidWaistSize, UpperWaistSize, UpperTorsoSize, TorsoHeight, NeckLength, BreastSize, ButtSize, ThighScale, CalfScale, UpperArmScale, LowerArmScale, NeckScale, HipWidth, LegsWidth, HeightOffset, FootRotation, BallRotation, Height
    }
}